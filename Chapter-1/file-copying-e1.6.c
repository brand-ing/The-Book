#include <stdio.h>
/* copy input to output; 2nd version */
int main()
{
    int c;
    if (c != EOF) {
        printf("The expression evaluated to 1.\n");
    } else {
        printf("The expression evaluated to 0.\n");
    }
    return 0;
}